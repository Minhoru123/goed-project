ANTHROPIC_API_KEY=sk-ant-...

# Optional: "updatable without developer" path.
# Publish your Google Sheet to the web as CSV (File -> Share -> Publish to web -> CSV)
# and paste the URL here. ETL will use it instead of the local .xlsx.
# RESOURCES_URL=https://docs.google.com/spreadsheets/d/e/.../pub?output=csv&gid=0
# COMPANIES_URL=https://docs.google.com/spreadsheets/d/e/.../pub?output=csv&gid=0
